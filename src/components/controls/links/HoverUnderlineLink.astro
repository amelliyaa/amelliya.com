---
const { href, isActive, ...rest } = Astro.props;
---

<a
  href={href}
  class:list={["hover-underline-link", isActive && "active"]}
  aria-current={isActive && "page"}
  {...rest}
>
  <slot />
</a>

<style>
  .hover-underline-link {
    display: inline-block;
    position: relative;
    margin: 0;
    border-radius: var(--border-radius-sm);
  }

  .hover-underline-link::after {
    content: "";
    position: absolute;
    left: 0;
    bottom: -0.25rem;
    block-size: 2px;
    inline-size: 100%;
    background-color: var(--color-complement);
    transform-origin: left;
    transform: scale(0);
  }

  .active {
    color: var(--color-primary-200) !important;
  }

  .active::before {
    content: "";
    position: absolute;
    z-index: -1;
    top: 50%;
    left: 50%;
    inline-size: 125%;
    block-size: 150%;
    border-radius: var(--border-radius-sm);
    outline: 2px solid var(--color-primary-400);
    transform: translate(-50%, -50%);
  }

  .hover-underline-link:hover,
  .hover-underline-link:focus {
    color: var(--color-primary-200);
  }

  :not(.active).hover-underline-link:hover::after,
  :not(.active).hover-underline-link:focus::after {
    transform: scale(100%);
  }

  :not(.active).hover-underline-link:active {
    outline: 2px solid var(--color-complement);
  }

  @media (prefers-reduced-motion: no-preference) {
    .hover-underline-link {
      transition: color var(--anim-length-short) ease-in-out;
    }

    .hover-underline-link::after {
      transition:
        opacity var(--anim-length-base),
        transform var(--anim-length-base);
    }
  }
</style>
